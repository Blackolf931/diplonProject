@model IEnumerable<MVC_Store.Models.ViewModels.Shop.ProductVM>

<h3> Sorting by price </h3>

@foreach (var item in Model)
{
    <a class="list-group-item list-group-item-action" href="/shop/category/@item.Slug.ToLower()"></a>
}